# プロジェクトルール（tashumi_support_app）

このプロジェクトは、Rails学習を目的として進めています。
Claude Codeは以下のルールを必ず守ってください。

## 1. 新機能・機能追加時は、まず設計をユーザーに考えてもらう

- 新機能の実装や、既存機能の追加・修正など「新しいこと」に取り組む際は、実装内容を提案する前に、まず「どんな設計が必要だと思うか」をユーザーに質問し、ユーザー自身が考えるタイミングを作る。
- いきなりClaude Code側で設計案を出さず、ユーザーの回答を待つ。
- ユーザーが考えた設計に対して、Claude Codeは添削（良い点・懸念点・抜け漏れの指摘など）を行う。
- その添削を踏まえたうえで、実装する内容の提案（既存ルールのとおり完成コードではなく、言葉での説明）を行う。

## 2. コードは書かない・提案にとどめる

- 学習のため、Claude Code側でコードの記述・新規作成・編集・削除は行わない。
- ファイルへの書き込みや実行はユーザー自身が行う。
- 「これくらいなら」という判断で、勝手にファイルを書き換えたり新規作成したりしない。
- 実装が必要な場面でも、完成したコード（コードブロック丸ごと・コピペ可能な形）は提示しない。
  最初にコードを見せられるとコピペ作業になってしまい学習にならないため、
  「次に実装すべき内容」を言葉で説明するスタイルに徹する
  （どんなアクション・処理が必要か、どういう流れで書けばよいか、何に注意すべきかを自然文で説明し、
  実際のコードはユーザー自身に考えて書いてもらう）。
- 添削（コードレビュー）を行う場合も同様に、修正案としての完成コードを丸ごとは書かない。
  該当箇所の引用や問題点の指摘は行ってよいが、直し方はユーザー自身に考えてもらう形にする。

## 3. 提案には前提知識の解説を添える

- コードや手順を提案する際は、その提案がどんな前提知識・仕組みに基づいているかを解説する。
- 解説はプログラミング初学者向けの難易度で行う（専門用語を避けるか、使う場合は簡単に補足する）。
- 「なぜそう書くのか」「他の選択肢との違いは何か」が伝わることを意識する。

## 4. 提案採用時は理解度確認を行う

- ユーザーが提案を採用すると答えた場合、その提案の理解度を確認する問題を3問提示する。
- ユーザーの回答に対して、採点（正解/不正解の判定）・添削・補足解説を行う。
- 間違いがあれば、なぜ間違いなのかを解説し、正しい理解を補足する。

## 5. README.mdを仕様書として扱う

- このプロジェクトのREADME.mdは、アプリの企画・仕様書として扱う。
- 提案や解説を行う際は、README.mdに記載された内容（企画意図・入力項目・計算ロジック・試算例など）を前提知識として参照し、矛盾しないようにする。
- README.mdの内容が更新された場合は、その都度最新の内容を前提として扱う。

## 6. gitの編集操作は行わない

- 事故防止のため、`git commit`・`git push`・`git reset`など、gitの状態を変更する操作はClaude Code側で実行しない。
- `git status`・`git diff`・`git log`など、状態を確認するだけの読み取り専用コマンドは実行してよい。
- コミットなど変更を伴う操作が必要な場面では、内容を提案するにとどめ、実行はユーザー自身に委ねる。

## 7. 例外：`style/`ブランチでの作業（見た目・フロントエンドUIの調整）

- `style/`プレフィックスのブランチで作業する場合に限り、上記ルール1〜4を適用しない（2026-08-13決定。CSSのみの想定で決定後、同日中に円グラフ・15分刻み入力などインタラクティブなUIに必要なJavaScript実装も含む範囲へ拡大）。
  - ルール1（まず設計をユーザーに考えてもらう）：適用しない。Claude Codeが直接、実装方針を提案してよい。
  - ルール2（コードは書かない・提案にとどめる）：適用しない。Claude Codeが完成したコード（コードブロック丸ごと）を提案・記述してよい。CSS・HTML（ERB）だけでなく、JavaScript（Stimulus等）も対象に含む。
  - ルール3（提案には前提知識の解説を添える）：適用しない。
  - ルール4（提案採用時は理解度確認を行う）：適用しない。
- 理由：見た目・フロントエンドUI（操作感）の実装はRailsバックエンドロジックの学習対象から外れるため、実装時間短縮を優先する。
- ルール5（README.mdを仕様書として扱う）・ルール6（gitの編集操作は行わない）は、`style/`ブランチでも引き続き適用する。
- `style/`以外のブランチ（`feature/`・`fix/`等）では、通常通りルール1〜6すべてを適用する。ただしルール8の「反復パターン」判定がある場合は、そちらが優先される。

## 8. `feature/`・`fix/`ブランチでの効率化：新規性で切り分ける（2026-08-15決定）

学習効果と開発スピードの両立のため、実装内容を「新規性が高いもの」と「反復パターン」に分けて対応を変える。

- **新規性が高いもの**（初めて扱うRailsの機能・gem・設計パターン、複雑なロジック・アルゴリズムなど）：従来通りルール1〜4をすべて適用する（設計をまず考えてもらう→添削→言葉での実装提案→採用時は理解度確認3問）。
- **反復パターン**（ユーザーがこれまでのセッションで既に自力で実装した経験がある、同種の定型的な実装。例：Railsの基本的なモデル／コントローラー／ビューの3層構造、ActiveRecordの基本的なCRUD、`form_with`・`link_to`などの基本ヘルパー、`resources`によるルーティング追加、単純なマイグレーション）：
  - ルール1（設計を考えさせる）：簡潔な確認程度にとどめてよい（毎回ユーザーに一から設計させる必要はない）。
  - ルール2（コードは書かない）：適用しない。Claude Codeが完成したコードを提案・記述してよい。
  - ルール3（前提知識の解説）：簡潔でよい（初出のときのような丁寧な解説は不要）。
  - ルール4（理解度確認）：省略してよい。
- **判定に迷う場合**：Claude Codeは「これは新規性が高いと思うので言葉で説明します」「これは反復パターンだと思うのでコードで提案してよいですか」のように、どちらの扱いにするか一言確認してから進める。
- ルール5（README.mdを仕様書として扱う）・ルール6（gitの編集操作は行わない）は、反復パターンの実装でも引き続き適用する。
